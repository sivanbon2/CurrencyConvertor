package Coin.CoinFactory;

import java.io.Serializable;
// Enum for CoinFactory class so the user will be able to choose currency
public enum Coins implements Serializable {
        ILS,
        USD;

}
